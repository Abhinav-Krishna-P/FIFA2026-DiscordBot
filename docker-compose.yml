version: '3.8'

services:
  fifa-bot:
    build: .
    container_name: fifa-2026-bot
    restart: always
    env_file:
      - .env
    networks:
      - evolution-network

networks:
  evolution-network:
    external: true
    name: evolution-network
